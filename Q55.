question-Write a program to print the following pattern:

   *
  ***
 *****
*******
 *****
  ***
   *
answer-
#include <stdio.h>

int main() {
    int i, j;

    // Upper half (including middle row)
    for (i = 1; i <= 4; i++) {
        // print spaces
        for (j = 1; j <= 4 - i; j++) {
            printf(" ");
        }
        // print stars
        for (j = 1; j <= 2 * i - 1; j++) {
            printf("*");
        }
        printf("\n");
    }

    // Lower half
    for (i = 3; i >= 1; i--) {
        // print spaces
        for (j = 1; j <= 4 - i; j++) {
            printf(" ");
        }
        // print stars
        for (j = 1; j <= 2 * i - 1; j++) {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
