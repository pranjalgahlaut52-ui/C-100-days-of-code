question-Write a program to take a string input. Change it to sentence case.
answer-#include <stdio.h>
#include <ctype.h>

int main() {
    char str[200];
    fgets(str, sizeof(str), stdin);

    // Convert entire string to lowercase first
    for (int i = 0; str[i] != '\0'; i++) {
        if (str[i] >= 'A' && str[i] <= 'Z')
            str[i] = str[i] + 32;
    }

    // Make first non-space character uppercase
    int i = 0;
    while (str[i] == ' ')  // skip leading spaces
        i++;

    if (str[i] >= 'a' && str[i] <= 'z')
        str[i] = str[i] - 32;

    printf("%s", str);
    return 0;
}
