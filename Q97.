question-Reverse each word in a sentence without changing the word order.
answer-#include <stdio.h>
#include <string.h>

int main() {
    char sentence[1000];

    printf("Enter a sentence: ");
    fgets(sentence, sizeof(sentence), stdin);

    int start = 0, end;
    for (int i = 0; ; i++) {
        if (sentence[i] == ' ' || sentence[i] == '\n' || sentence[i] == '\0') {
            end = i - 1;
            // Reverse the word from start to end
            while (start < end) {
                char temp = sentence[start];
                sentence[start] = sentence[end];
                sentence[end] = temp;
                start++;
                end--;
            }
            if (sentence[i] == '\0') break;
            start = i + 1; // Move to next word
        }
    }

    printf("Sentence after reversing each word: %s", sentence);

    return 0;
}
