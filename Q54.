question-Write a program to print the following pattern:
*
***
*****
*******
*********
*******
*****
***
*

answer-#include <stdio.h>

int main() {
    int i, j;

    // Upper part (1 to 5 rows)
    for (i = 1; i <= 5; i++) {
        int stars = 2 * i - 1;
        for (j = 1; j <= stars; j++) {
            printf("*");
        }
        printf("\n");
    }

    // Lower part (4 to 1 rows)
    for (i = 4; i >= 1; i--) {
        int stars = 2 * i - 1;
        for (j = 1; j <= stars; j++) {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
