question-Print all sub-strings of a string.
answer-#include <stdio.h>
#include <string.h>

int main() {
    char str[1000];
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin);

    // Remove newline if present
    str[strcspn(str, "\n")] = '\0';

    int n = strlen(str);

    printf("All substrings of the string are:\n");

    for (int i = 0; i < n; i++) {       // Starting index
        for (int j = 1; j <= n - i; j++) { // Length of substring
            for (int k = i; k < i + j; k++) {
                printf("%c", str[k]);
            }
            printf("\n");
        }
    }

    return 0;
}
